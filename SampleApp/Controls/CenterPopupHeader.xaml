<UserControl
    x:Class="SampleApp.Controls.CenterPopupHeader"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SampleApp.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <TextBlock Text="测试标题" FontSize="20" VerticalAlignment="Center"/>
        <Button Style="{StaticResource EllipsisButton}" Content="关" VerticalAlignment="Center" Grid.Column="1" x:Name="CloseButton"
                            Click="CloseButton_Click"/>
    </Grid>
</UserControl>
